% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutator-return.R
\name{replace_return_value}
\alias{replace_return_value}
\title{Replace the value in explicit return() calls}
\usage{
replace_return_value(replacement = "NULL")
}
\arguments{
\item{replacement}{Raw R source text to substitute as the return value.
Defaults to \code{"NULL"}. Examples: \code{"NA"} inserts the missing value \code{NA};
\code{'"NULL"'} (inner quotes) inserts the string \code{"NULL"}; \code{'"NA"'} inserts
the string \code{"NA"} rather than the missing value.}
}
\value{
A \link{Mutator} object.
}
\description{
Replaces the argument of every \code{return(expr)} with a fixed value (default
\code{"NULL"}). Tests that only check that a function returns \emph{something} without
asserting the value will not kill these mutants.
}
\details{
Only explicit \code{return()} calls are targeted. Implicit returns (the last
expression of a function body) are not affected.
}
\examples{
replace_return_value()
replace_return_value("NA")
}
