% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutator-operator.R
\name{operator}
\alias{operator}
\title{Mutate a binary operator}
\usage{
operator(from, to)
}
\arguments{
\item{from}{The operator to replace (e.g. \code{"+"}, \code{"=="}, \code{">"}).}

\item{to}{The replacement operator.}
}
\value{
A \link{Mutator} object.
}
\description{
Produces one mutant per occurrence of \code{from} in the source file, replacing
it with \code{to}. A surviving mutant means your tests cannot distinguish the
original operator from the replacement — pointing at the missing assertion
or input value.
}
\details{
Use this when you need a specific swap not covered by the preset collections
(\code{\link[=arithmetic_operators]{arithmetic_operators()}}, \code{\link[=comparison_operators]{comparison_operators()}}, \code{\link[=logical_operators]{logical_operators()}}).
}
\examples{
operator("+", "-")
operator("==", "!=")
operator(">", ">=")  # probe the strict vs. non-strict boundary
}
\seealso{
\code{\link[=comparison_operators]{comparison_operators()}}, \code{\link[=arithmetic_operators]{arithmetic_operators()}}, \code{\link[=logical_operators]{logical_operators()}}
for ready-made preset lists.

\code{vignette("mutators", package = "muttest")} for the full operator
reference with examples of what each preset catches.

\code{vignette("interpreting-results", package = "muttest")} to learn how to
read surviving mutants and strengthen the tests they expose.
}
